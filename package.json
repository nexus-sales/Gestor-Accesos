{
  "name": "gestor-accesos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start": "node server.js",
    "dev": "npm run build && node dev-server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
